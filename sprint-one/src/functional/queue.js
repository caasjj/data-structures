var makeQueue = function(){
  var instance = {}, storage = {}, head=0, size=0;

  // Implement the methods below
  instance.enqueue = function(value){
    size++;
  };

  instance.dequeue = function(){
   
  };

  instance.size = function(){
    return size;
  };

  return instance;
};

// c b a